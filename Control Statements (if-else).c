#include <stdio.h>

int main() {
    int num = 10;
    
    if (num > 5) {
        printf("Number is greater than 5.\n");
    } else {
        printf("Number is not greater than 5.\n");
    }

    return 0;
}
